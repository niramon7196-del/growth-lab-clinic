sed -i 's/<div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-900\/60 backdrop-blur-xs">/& \n            onClick={(e) => { if (e.target === e.currentTarget) { setShowManualCheckInModal(false); setShowAddApptModal(false); setShowEditProfileModal(false); } }}/g' src/components/UserWorkspace.tsx
