perl -0777 -pi -e 's/export default function WelcomeModal\(\{\ onClose\,\ onNavigate\ \}\:\ WelcomeModalProps\)\ \{/export default function WelcomeModal({ onClose, onNavigate }: WelcomeModalProps) {\n  React.useEffect(() => {\n    const handleKeyDown = (e: KeyboardEvent) => {\n      if (e.key === '\''Escape'\'') onClose();\n    };\n    window.addEventListener('\''keydown'\'', handleKeyDown);\n    return () => window.removeEventListener('\''keydown'\'', handleKeyDown);\n  }, [onClose]);/g' src/components/WelcomeModal.tsx
perl -0777 -pi -e 's/<div className="fixed inset-0 bg-slate-950\/80 backdrop-blur-md z-50 flex items-center justify-center p-4">/<div className="fixed inset-0 bg-slate-950\/80 backdrop-blur-md z-50 flex items-center justify-center p-4" onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}>/g' src/components/WelcomeModal.tsx
perl -0777 -pi -e 's/className="bg-white w-full max-w-lg rounded-\[32px\] overflow-hidden shadow-2xl border border-white text-left p-8 space-y-6 relative"/className="bg-white w-full max-w-lg rounded-[32px] overflow-hidden shadow-2xl border border-white text-left p-8 space-y-6 relative max-h-[90vh] overflow-y-auto"/g' src/components/WelcomeModal.tsx
