perl -0777 -pi -e 's/export default function InitialAdminSetupModal\(\{\n  isOpen,\n  onClose,\n  onSuccess,\n  titleOverride,\n  isInitialSetup \= true\n\}\:\ InitialAdminSetupModalProps\)\ \{/export default function InitialAdminSetupModal({\n  isOpen,\n  onClose,\n  onSuccess,\n  titleOverride,\n  isInitialSetup = true\n}: InitialAdminSetupModalProps) {\n  React.useEffect(() => {\n    const handleKeyDown = (e: KeyboardEvent) => {\n      if (e.key === '\''Escape'\'' && onClose) onClose();\n    };\n    window.addEventListener('\''keydown'\'', handleKeyDown);\n    return () => window.removeEventListener('\''keydown'\'', handleKeyDown);\n  }, [onClose]);/g' src/components/InitialAdminSetupModal.tsx
perl -0777 -pi -e 's/<div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-900\/60 backdrop-blur-xs">/<div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-900\/60 backdrop-blur-xs" onClick={(e) => { if (e.target === e.currentTarget && onClose) onClose(); }}>/g' src/components/InitialAdminSetupModal.tsx
perl -0777 -pi -e 's/className="bg-white rounded-3xl shadow-2xl max-w-md w-full overflow-hidden"/className="bg-white rounded-3xl shadow-2xl max-w-md w-full overflow-hidden max-h-[90vh] overflow-y-auto"/g' src/components/InitialAdminSetupModal.tsx
