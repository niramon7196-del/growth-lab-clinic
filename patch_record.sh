sed -i 's/<div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-900\/60 backdrop-blur-xs overflow-y-auto">/<div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-slate-900\/60 backdrop-blur-xs overflow-y-auto" onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}>/g' src/components/RecordDetailModal.tsx
sed -i 's/const RecordDetailModal: React.FC<RecordDetailModalProps> = ({ record, onClose }) => {/&\n  React.useEffect(() => {\n    const handleKeyDown = (e: KeyboardEvent) => {\n      if (e.key === '\''Escape'\'') onClose();\n    };\n    window.addEventListener('\''keydown'\'', handleKeyDown);\n    return () => window.removeEventListener('\''keydown'\'', handleKeyDown);\n  }, [onClose]);/g' src/components/RecordDetailModal.tsx
